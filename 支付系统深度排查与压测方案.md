# 支付系统深度排查与压测方案（2026-08-05）

> 范围：最近 3 天（08-02 ~ 08-05）全部提交，重点是新增的支付/分成系统与随之而来的资金安全加固。
> 结论先行：**支付模块的资金安全设计是扎实的**——验签 fail-closed、金额比对、原子抢占、幂等门控、悲观锁、状态机失败区分都已落地。发现 1 个**潜伏级**迁移一致性问题（已附修复），以及若干运维/容量注意点。压测**可以进一步做**，且已交付可直接运行的并发压测脚本。

> 📌 **更新说明（2026-08-05 晚）**：报告中提到的修复迁移 `0016`（清理遗留索引）、`0017`（`ref_id` UUID→TEXT）**已折入 `0010` 并重命名删除**——因为生产从未部署、任何库都没到过错误中间态。部署只需跑 `0010`→`0013`（详见 `DEPLOY-PAYMENTS-PLAN.md`）。下文对 `0016/0017` 的描述仅为当时审计记录，现已内置于 `0010`。

---

## 一、近 3 天变更全貌

按 `git log --since=2026-08-02` 统计，支付相关提交约 40 个，可分为四波：

| 波次 | 主题 | 关键 commit |
|---|---|---|
| 1 | 支付系统地基 + 创作者会员制上线 | `9356296` 创作者定价写接口、`9e43c87` 创作者会员制、`af2bc18` 迁移链精简 0010→0011→0013 |
| 2 | 资金安全大规模审计加固 | `eb4827e` 验签 fail-closed+原子防双花+退款闭环+0元拦截、`2755e53` 对照微信官方质检清单修 6 项 |
| 3 | 并发/双入账漏洞修复 | `95d2753` 余额双入账/退款少扣、`5c1cfcc` 原子级幂等大修、`85cb459` 提现防双扣、`e22483b` 提现误解冻修复 |
| 4 | 收尾优化 + 文档 | `53d955a` freeze 原子条件递减、`8e1df03` 4 项低风险优化、`461186e` 付费墙 fail-closed + 退款并发原子化、`0014/0015` 余额流水幂等索引 |

整体方向是对的：**从「能跑通付款」转向「付了钱不会算错、不会被薅、并发不双发」**。

---

## 二、资金安全铁律逐项核对

### 铁律① 回调验签 fail-closed ✅
`wechat-pay.service.ts:112 verifySignature`：平台证书缺失 → 直接 `return false` 拒绝（旧实现曾 `return true` 放行，等于资金后门）；时间戳超 5 分钟按重放攻击拒绝；RSA-SHA256 验签失败亦拒绝。支付/退款/转账三个回调入口都走了它（`wechat-notify.controller.ts:60`、`orders.service.ts:273`、`refund.service.ts:178`）。**正确。**

### 铁律② 金额比对（发货前）✅
`orders.service.ts:302` 在标记 PAID **之前**比对 `decrypted.amount.total === payment.amount_cents`；`syncFromWechat`（`orders.service.ts:433`）查单路径也做了同一比对。**正确。**

### 铁律③ 幂等（原子抢占）✅
`orders.service.ts:338 markPaymentPaid`：`UPDATE ... SET status='PAID' WHERE id=? AND status<>'PAID'`，以 `affected>0` 判定抢占成功，并发回调与轮询查单只会一路继续发货。**正确。**

### 并发双入账 / 双扣：原子门控 ✅（核心）
`balance.service.ts:85 credit` 用 `INSERT ... ON CONFLICT (user_id,ref_id,biz_type,direction) WHERE ref_id IS NOT NULL DO NOTHING RETURNING id` 做原子门控——**只有流水真正插入的那一路才 `increment` 余额**，从根上消除「先查后插」非原子导致的双入账（HIGH-1）。`debitForRefund`（`:159`）同源，且用**退款单 id** 作幂等键，避免同订单多次部分退款被唯一索引吞掉导致创作者少扣（HIGH-2）。**设计正确。**

### 并发双冻结：原子条件递减 ✅
`balance.service.ts:116 freeze`：`UPDATE ... SET available=available-$1, frozen=frozen+$1 WHERE user_id=$2 AND available_cents>=$1 RETURNING`。单条 SQL 内校验余额充足并扣减，杜绝「先读余额再减」的 TOCTOU 透支（同一用户并发审批多笔提现会双扣）。**正确。**

### 提现双打款：原子抢占 + 异步终态闭环 ✅
`admin-payments.service.ts:132 approveWithdrawal`：先 `UPDATE ... SET status='REVIEWING' WHERE id=? AND status='PENDING'` 原子抢占，并发双击只有一路；微信转账是异步接口，**仅 `SUCCESS` 终态才 `completeWithdrawal` 核销余额**，其余置 `PROCESSING` 由回调/定时查单收口。`completeWithdrawal`/`failWithdrawal` 把「状态翻转 + 余额变动」放进同一 DB 事务，极端崩溃可回滚重试（LOW-1）。**正确。**

### 退款闭环：悲观锁 + 状态机失败区分 ✅
`refund.service.ts:82 createRefund`：悲观锁 `SELECT ... FOR UPDATE` 串行化同订单并发退款，杜绝超退；`finalizeRefund`（`:219`）各步骤幂等，SUCCESS 标记置后。失败区分精准：4xx 业务拒绝 → 标 FAILED 释放额度；超时/5xx/不确定 → 用原 `out_refund_no` 查单核实，绝不盲目标失败放任重试换新单号（换单号=可能退两次）。**正确，符合微信官方规范。**

### 授权闸门：仅以 payments.PAID 为事实源 ✅
`refund.service.ts:63`：退款前必须存在 `payments.status='PAID'`，`order.status` 仅是派生状态，杜绝「未支付却可退款」。**纵深防御到位。**

---

## 三、发现的问题（按严重度）

### 🔴 HIGH（资金/可用性）— `balance_transactions.ref_id` 类型与业务键不一致
- **现象**：实体 `BalanceTransaction.ref_id` 定义为 `uuid`（`payments.entity.ts:346`），迁移 0010 建的列也是 `UUID`；但 `BalanceService.credit`（`balance.service.ts:79`，订单 `orders.service.ts:379`）传入的 ref_id 是 **`${order.id}:${item.id}`（复合字符串，如 `uuid:uuid`，不是合法 UUID）**。退款路径 `debitForRefund` 传的是 `refund.id`（合法 UUID），不受影响。
- **影响**：若生产库按当前 0010 DDL 确为 `UUID`，则每笔销售入账的 `INSERT INTO balance_transactions ... VALUES (..., $4=ref_id)` 会报 **`invalid input syntax for type uuid`**。该 INSERT 在 `deliver()` 中位于 `grantPurchase`（买家拿权益）**之后**，因此结果是：**买家已拿到技能，但 `credit` 抛错 → `deliver` 抛错 → 订单永久卡在 `PAID` → 创作者余额永不增加**，且前端轮询/微信重发回调会反复重试、永远失败。这是创作者分文未得的资损级故障。
- **命中条件**：库由「当前版 0010」迁移创建即触发；若历史某次 0010 曾为 `TEXT`（架构漂移），该环境暂未爆发但全新部署会复发。**无论哪种，都必须统一为 TEXT。**
- **修复**：已新增 **`migrations/0017_alter_balance_tx_ref_id_text.sql`**（`ALTER TABLE balance_transactions ALTER COLUMN ref_id TYPE text USING ref_id::text;`，幂等可重跑），并同步将实体改为 `type: 'text'`。唯一索引 `idx_balance_tx_idempotent` 对 TEXT 同样成立。
- **立即核查**：`SELECT data_type FROM information_schema.columns WHERE table_name='balance_transactions' AND column_name='ref_id';` —— 若是 `uuid` 必须立刻跑 0017；若是 `text` 说明存在漂移，仍建议跑 0017 固化。

### 🟡 LOW（潜伏）— 0014/0015 遗留过宽索引
- **现象**：`0014_balance_tx_idempotent.sql` 建了 `uq_balance_tx_ref(ref_id, biz_type, direction) WHERE ref_id IS NOT NULL`（缺 `user_id`）；`0015` 又建了正确的 `idx_balance_tx_idempotent(user_id, ref_id, biz_type, direction)`，但 `0015` 只 `DROP` 了同名旧索引，**未 DROP 0014 的 `uq_balance_tx_ref`**。
- **影响（当前为潜伏，非线上事故）**：当前订单每行仅 1 个 item、ref_id 全局唯一（`order.id:item.id`），两索引都不会冲突，线上无影响。但一旦未来出现「同一订单含多个同卖家 item」的多 item 订单，第二个 item 的 `credit()` INSERT 会同时撞上 `uq_balance_tx_ref`（不含 user_id）→ 抛 23505，而 `ON CONFLICT` 仅抑制正确的那一索引 → `deliver` 永久抛错、订单卡 PAID 反复重试、买家拿不到权益。
- **修复**：已新增 **`migrations/0016_drop_legacy_balance_tx_index.sql`**（`DROP INDEX IF EXISTS uq_balance_tx_ref;`），幂等可重跑。未应用过 0014 的环境该 DROP 为 no-op。

### 🟡 INFO — 生产库迁移落实情况需人工核查
- 旧内存记录显示 0010/0011 曾「服务器待执行」。请在生产库确认：
  1. `idx_balance_tx_idempotent` 存在（并发双入账防护的基石）；
  2. `uq_balance_tx_ref` **不存在**（即 0016 已跑，或 0014 从未应用）；
  3. `creator_subscriptions` 的 `uq_cs_user_target` 唯一索引存在（会员续订 23505 重试依赖它）。
- 核查 SQL：`\d balance_transactions` 看索引；`\d creator_subscriptions` 看唯一约束。

### 🟡 INFO — DB 连接池容量（容量隐患，非正确性）
- `createOrder` 同步 `fetch` 微信（10s 超时），`handleNotify`→`deliver` 每笔回调串行若干次查库。高并发回调突发时，若 TypeORM `poolSize` 偏小，请求会阻塞在拿连接上，体现为延迟上升（不影响正确性，因所有闸门都是 DB 原子）。**压测时务必监控 `pg_stat_activity` 中 `idle in transaction` / 等待连接数**，必要时调大 `extra.max`（建议 ≥ 20）。

### 🟢 OK — 多实例部署安全
- 所有关键闸门（markPaymentPaid / approveWithdrawal 抢占 / credit 门控 / createRefund 悲观锁）都是 **DB 级原子**，因此 `OrdersService`/`AdminPaymentsService` 里的 `setInterval` 定时器即使在多副本水平扩容下也安全（每副本各自跑 closeExpired / syncProcessingWithdrawals，但收口动作靠原子 UPDATE 去重）。**设计上已支持多实例。**

---

## 四、支付模块能否进一步压测？

**能，而且应该。** 但要分清「能压什么 / 不能压什么」：

### ❌ 不能压（或禁止压）
- **真实微信支付 API**：限流、真金白银、需真实商户证书与签名。压测生产/真实微信 = 资损 + 封号。
- **生产环境**：任何压测都只在 staging / 隔离库进行。

### ✅ 能压（价值最高）
1. **资金不变量并发压测**（本次重点交付）：在隔离 Postgres 上，复刻服务内原子 SQL，对「并发双入账 / 双冻结 / 超额退款 / 并发双发货」做高并发冲击，断言余额与流水精确对账。**这直接验证你花大力气修的并发漏洞是否真的扛得住。**
2. **HTTP 层吞吐**（k6）：对 `/api/pay/orders` 与模拟 `/api/pay/wechat/notify` 做 VU 压测，看 p95 延迟与错误率。需在 staging 把 `WechatPayService` 替换为内存桩（不下真实微信）。
3. **回调洪峰幂等**：高并发重放同一 `out_trade_no` 的回调，验证「只发货一次、余额只入一次」。

### 已交付的压测工具
| 文件 | 作用 | 运行 |
|---|---|---|
| `scripts/load-test-payments.mjs` | 资金不变量并发压测（4 场景 + 不变量断言 + p95 延迟） | 见下 |
| `docker-compose.load.yml` | 隔离 Postgres（端口 5433，库 `loadtest_pay`） | `docker compose -f docker-compose.load.yml up -d` |
| `scripts/k6-payments.js` | HTTP 层 VU 压测（下单 + 回调） | `k6 run -e BASE_URL=... -e BEARER=... scripts/k6-payments.js` |

### 运行步骤（资金不变量压测）
```bash
# 1) 起隔离库
docker compose -f docker-compose.load.yml up -d
# 2) 跑（默认 64 并发 / 2000 任务；可改环境变量）
node scripts/load-test-payments.mjs
#   或单场景：SCENARIO=refund node scripts/load-test-payments.mjs
#   或调并发：CONCURRENCY=128 TASKS=5000 node scripts/load-test-payments.mjs
```
脚本自建最小表结构、自建 `idx_balance_tx_idempotent`（与生产一致），**绝不触碰业务库**。退出码 0=全 PASS、1=有 FAIL。

### 预期结果
- 场景①/①b：并发 80 路冲击下，余额仍只入账 1 次、流水只 1 行 → PASS。
- 场景②：并发审批仅 1 路生效、余额仅冻结 1 次、微信转账仅发起 1 次 → PASS。
- 场景③：并发全额退款仅 1 笔成功、退款总额 ≤ 实付、余额不为负 → PASS。
- 场景④：吞吐下额度仍精确对账（available == 成功笔数）→ PASS。

---

## 五、下一步建议
1. **立即（P0）**：先在生产库跑 `SELECT data_type ...` 核查 `ref_id` 真实类型；若为 `uuid`，马上执行 `0017_alter_balance_tx_ref_id_text.sql` 并在代码侧已同步实体为 text。随后执行 `0016_drop_legacy_balance_tx_index.sql`；并核查第三节 INFO 项。
2. **压测**：在 staging（微信已桩）跑 `load-test-payments.mjs` + k6，把不变量 PASS 与 p95 延迟作为上线门禁。
3. **容量**：压测时观察 `pg` 连接池与事件循环，按需调大 `poolSize`；评估 `closeExpired`/`syncProcessingWithdrawals` 在大量积压订单时的串行网络耗时（可改批/并行）。
4. **可观测**：`AdminPaymentsService.reconcile()` 已具备三向对账，建议接入定时任务 + 告警（diff≠0 即报警），把「账对不上」从人工发现变自动发现。
